name: AI Photo Locked Visual 6-Round Pre-OTA QA

on:
  push:
    branches: [ai-photo-real-canva-ai-generator-correction]
    paths:
      - 'fresh-rebuild/src/features/apps/ai-photo-locked-visual-v1.js'
      - 'fresh-rebuild/src/features/apps/ai-photo-locked-reference-assets-v1.js'
      - 'fresh-rebuild/src/features/apps/ai-photo-studio-review.js'
      - 'fresh-rebuild/assets/visuals/ai-photo-locked-styles.webp'
      - 'fresh-rebuild/assets/visuals/ai-photo-locked-featured.webp'
      - 'fresh-rebuild/assets/visuals/ai-photo-locked-recent.webp'
      - 'fresh-rebuild/assets/visuals/ai-photo-locked-reference-manifest.json'
      - 'NexusNovaAndroid/app/src/main/assets/www/src/features/apps/ai-photo-locked-visual-v1.js'
      - 'NexusNovaAndroid/app/src/main/assets/www/src/features/apps/ai-photo-locked-reference-assets-v1.js'
      - 'NexusNovaAndroid/app/src/main/assets/www/src/features/apps/ai-photo-studio-review.js'
      - 'NexusNovaAndroid/app/src/main/assets/www/assets/visuals/ai-photo-locked-styles.webp'
      - 'NexusNovaAndroid/app/src/main/assets/www/assets/visuals/ai-photo-locked-featured.webp'
      - 'NexusNovaAndroid/app/src/main/assets/www/assets/visuals/ai-photo-locked-recent.webp'
      - 'tools/ai-photo-locked-visual/harness.html'
      - 'tools/ai-photo-locked-visual/render-qa.mjs'
      - 'tools/ai-photo-locked-visual/render-qa.sh'
      - 'AI-PHOTO-WORK-HANDOFF.md'
      - '.github/workflows/ai-photo-locked-visual-preota-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  locked_visual_qa:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout locked visual branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 'QA 1/6 — Exact scope + protected modules'
        shell: bash
        run: |
          set -euo pipefail
          BASE=8ac25ef4c600d3e599858d4c207bf2ec131c18a9
          allowed='^(fresh-rebuild/src/features/apps/ai-photo-locked-visual-v1\.js|fresh-rebuild/src/features/apps/ai-photo-locked-reference-assets-v1\.js|fresh-rebuild/src/features/apps/ai-photo-studio-review\.js|fresh-rebuild/assets/visuals/ai-photo-locked-(styles|featured|recent)\.webp|fresh-rebuild/assets/visuals/ai-photo-locked-reference-manifest\.json|NexusNovaAndroid/app/src/main/assets/www/src/features/apps/ai-photo-locked-(visual-v1|reference-assets-v1)\.js|NexusNovaAndroid/app/src/main/assets/www/src/features/apps/ai-photo-studio-review\.js|NexusNovaAndroid/app/src/main/assets/www/assets/visuals/ai-photo-locked-(styles|featured|recent)\.webp|tools/ai-photo-locked-visual/(harness\.html|render-qa\.(mjs|sh))|AI-PHOTO-WORK-HANDOFF\.md|\.github/workflows/ai-photo-locked-visual-preota-ci\.yml)$'
          unexpected="$(git diff --name-only "$BASE" "$GITHUB_SHA" -- | grep -Ev "$allowed" || true)"
          if [ -n "$unexpected" ]; then
            echo '::error::Unexpected file changed:' >&2
            echo "$unexpected" >&2
            exit 1
          fi
          for path in \
            fresh-rebuild/assets/styles/ai-photo-route-fullscreen.css \
            fresh-rebuild/src/features/apps/app-screen.js \
            fresh-rebuild/src/features/apps/ai-photo-studio-flagship.js \
            fresh-rebuild/src/features/apps/ai-photo-focus-interaction.js \
            fresh-rebuild/src/features/apps/ai-photo-phone-feedback-v1.js \
            fresh-rebuild/src/features/apps/ai-photo-canva-workspace-v3.js \
            fresh-rebuild/src/features/apps/ai-photo-puter-generator.js \
            fresh-rebuild/src/features/apps/ai-photo-studio-ai.js \
            NexusNovaAndroid/app/src/main/java/com/nexusnova/app/NexusPuterPopupManager.kt \
            fresh-rebuild/src/features/apps/premium-qibla-safe-v2.js \
            fresh-rebuild/src/features/apps/premium-prayer-safe.js \
            fresh-rebuild/src/features/apps/pakistan-suite.js \
            fresh-rebuild/src/features/apps/news-suite.js \
            fresh-rebuild/src/features/apps/articles-suite.js \
            fresh-rebuild/src/features/apps/live-feed-premium-skin.js \
            fresh-rebuild/src/features/apps/batch4-tools-17-20-fullscreen.js \
            fresh-rebuild/assets/styles/batch4-tools-17-20-fullscreen.css; do
            test "$(git rev-parse "$BASE:$path")" = "$(git hash-object "$path")" || { echo "::error::Protected file changed: $path" >&2; exit 1; }
          done
          echo 'QA1 PASS — exact locked visual scope only; protected modules unchanged.'

      - name: 'QA 2/6 — JavaScript syntax + render wiring'
        shell: bash
        run: |
          set -euo pipefail
          V=fresh-rebuild/src/features/apps/ai-photo-locked-visual-v1.js
          A=fresh-rebuild/src/features/apps/ai-photo-locked-reference-assets-v1.js
          R=fresh-rebuild/src/features/apps/ai-photo-studio-review.js
          node --check "$V"
          node --check "$A"
          node --check "$R"
          node --check tools/ai-photo-locked-visual/render-qa.mjs
          bash -n tools/ai-photo-locked-visual/render-qa.sh
          grep -Fq "installAiPhotoLockedVisualV1(root)" tools/ai-photo-locked-visual/harness.html
          grep -Fq "installAiPhotoLockedReferenceAssetsV1(root)" tools/ai-photo-locked-visual/harness.html
          grep -Fq 'installAiPhotoLockedVisualV1' "$R"
          grep -Fq 'installAiPhotoLockedReferenceAssetsV1' "$R"
          grep -Fq 'review-v8-locked-reference' "$R"
          grep -Fq 'lockedReferenceCleanup' "$R"
          grep -Fq 'nxps-locked-visual' "$V"
          grep -Fq 'nxlock-home' "$V"
          grep -Fq 'nxlock-generator' "$V"
          echo 'QA2 PASS — syntax and exact-reference render wiring.'

      - name: 'QA 3/6 — Locked Home visual + reference pixels'
        shell: bash
        run: |
          set -euo pipefail
          V=fresh-rebuild/src/features/apps/ai-photo-locked-visual-v1.js
          A=fresh-rebuild/src/features/apps/ai-photo-locked-reference-assets-v1.js
          grep -Fq '⌂ Home' "$V"
          grep -Fq 'NexusNova AI Photo Studio' "$V"
          grep -Fq 'Create • Edit • Design • All in One' "$V"
          grep -Fq 'NEXUSNOVA CREATIVE AI' "$V"
          grep -Fq 'AI GENERATION' "$V"
          grep -Fq '1000+ TEMPLATES' "$V"
          grep -Fq 'PRO EDITOR' "$V"
          grep -Fq 'What do you want to create?' "$V"
          grep -Fq 'Create with AI' "$V"
          grep -Fq 'Edit a Photo' "$V"
          grep -Fq '>Templates<' "$V"
          grep -Fq 'My Creations' "$V"
          grep -Fq 'AI Tools' "$V"
          grep -Fq 'Favorites' "$V"
          grep -Fq '>Saved<' "$V"
          grep -Fq 'Featured templates' "$V"
          grep -Fq 'Quick tools' "$V"
          grep -Fq 'Recent creations' "$V"
          grep -Fq 'Open projects' "$V"
          grep -Fq "'instagram-stories'" "$V"
          grep -Fq 'grid-auto-columns:21.25%' "$V"
          grep -Fq '.nxlock-home{position:absolute;inset:0' "$V"
          grep -Fq 'overflow:hidden' "$V"
          grep -Fq 'ai-photo-locked-featured.webp' "$A"
          grep -Fq 'ai-photo-locked-recent.webp' "$A"
          test -s fresh-rebuild/assets/visuals/ai-photo-locked-featured.webp
          test -s fresh-rebuild/assets/visuals/ai-photo-locked-recent.webp
          file fresh-rebuild/assets/visuals/ai-photo-locked-featured.webp | grep -Fq 'Web/P image'
          file fresh-rebuild/assets/visuals/ai-photo-locked-recent.webp | grep -Fq 'Web/P image'
          node - fresh-rebuild/assets/visuals/ai-photo-locked-featured.webp 692x183 fresh-rebuild/assets/visuals/ai-photo-locked-recent.webp 680x124 <<'NODE'
          const fs=require('fs');
          const args=process.argv.slice(2);
          for(let i=0;i<args.length;i+=2){
            const [path,expected]=[args[i],args[i+1]];
            const b=fs.readFileSync(path);
            if(b.toString('ascii',0,4)!=='RIFF'||b.toString('ascii',8,12)!=='WEBP'||b.toString('ascii',12,16)!=='VP8L'||b[20]!==0x2f)throw new Error(`${path}: invalid lossless WebP`);
            const width=1+b[21]+((b[22]&0x3f)<<8);
            const height=1+(b[22]>>6)+(b[23]<<2)+((b[24]&0x0f)<<10);
            if(`${width}x${height}`!==expected)throw new Error(`${path}: expected ${expected}, got ${width}x${height}`);
          }
          NODE
          echo '642e17a71f1224d341f48a4a08de5ce96a05c5b5015518e726a67b397d36cfe6  fresh-rebuild/assets/visuals/ai-photo-locked-featured.webp' | sha256sum -c -
          echo '4b059de3f4d5a61eb67804c4e680953aade1f696b80b215f75ced1f514fad323  fresh-rebuild/assets/visuals/ai-photo-locked-recent.webp' | sha256sum -c -
          grep -Fq 'bc54e5535786bf5a0a2977cb6788db7725bec444afa18c72ae624c24a304f30a' fresh-rebuild/assets/visuals/ai-photo-locked-reference-manifest.json
          grep -Fq '"generated": false' fresh-rebuild/assets/visuals/ai-photo-locked-reference-manifest.json
          echo 'QA3 PASS — Home hierarchy plus exact user-approved Featured/Recent crop bytes.'

      - name: 'QA 4/6 — Locked AI Generator + exact Creative Styles pixels'
        shell: bash
        run: |
          set -euo pipefail
          V=fresh-rebuild/src/features/apps/ai-photo-locked-visual-v1.js
          A=fresh-rebuild/src/features/apps/ai-photo-locked-reference-assets-v1.js
          grep -Fq 'AI Image Generator' "$V"
          grep -Fq 'Create stunning images with AI' "$V"
          grep -Fq 'Describe your image' "$V"
          grep -Fq 'Creative Styles' "$V"
          grep -Fq 'Quality' "$V"
          grep -Fq 'Style Strength' "$V"
          grep -Fq '✦ Generate with AI' "$V"
          grep -Fq "['square','□','1:1','Square']" "$V"
          grep -Fq "['portrait','▯','4:5','Portrait']" "$V"
          grep -Fq "['story','▯','9:16','Story']" "$V"
          grep -Fq "['wide','▭','16:9','Landscape']" "$V"
          grep -Fq "option.value==='custom'" "$V"
          for label in '3D' 'Realistic' 'Anime' 'Cinematic' 'Digital Art' 'Portrait' 'Product' 'Logo'; do grep -Fq "'$label'" "$V"; done
          grep -Fq 'ai-photo-locked-styles.webp' "$A"
          grep -Fq 'background-size:800% 100%' "$A"
          grep -Fq '.nxlock-generator{position:relative!important}' "$A"
          test -s fresh-rebuild/assets/visuals/ai-photo-locked-styles.webp
          file fresh-rebuild/assets/visuals/ai-photo-locked-styles.webp | grep -Fq 'Web/P image'
          node - fresh-rebuild/assets/visuals/ai-photo-locked-styles.webp 744x88 <<'NODE'
          const fs=require('fs');
          const [path,expected]=process.argv.slice(2);
          const b=fs.readFileSync(path);
          if(b.toString('ascii',0,4)!=='RIFF'||b.toString('ascii',8,12)!=='WEBP'||b.toString('ascii',12,16)!=='VP8L'||b[20]!==0x2f)throw new Error(`${path}: invalid lossless WebP`);
          const width=1+b[21]+((b[22]&0x3f)<<8);
          const height=1+(b[22]>>6)+(b[23]<<2)+((b[24]&0x0f)<<10);
          if(`${width}x${height}`!==expected)throw new Error(`${path}: expected ${expected}, got ${width}x${height}`);
          NODE
          echo '4c66c16a95ab61d27e619900e7a4096cd790fbf0313476341fe7a34f9e47b845  fresh-rebuild/assets/visuals/ai-photo-locked-styles.webp' | sha256sum -c -
          grep -Fq '1bc28bd18dfb94b640c288648bc45ea70d4b3c38d4149abc082a507da6bf394f' fresh-rebuild/assets/visuals/ai-photo-locked-reference-manifest.json
          grep -Fq 'RIGHT-HAND screen marked FIXED LAYOUT (Final Setup) only' fresh-rebuild/assets/visuals/ai-photo-locked-reference-manifest.json
          echo 'QA4 PASS — generator contract plus exact user-approved Creative Styles crop bytes.'

      - name: 'QA 5/6 — Navigation + Puter + fullscreen behavior contract'
        shell: bash
        run: |
          set -euo pipefail
          V=fresh-rebuild/src/features/apps/ai-photo-locked-visual-v1.js
          P=fresh-rebuild/src/features/apps/ai-photo-studio-ai.js
          G=fresh-rebuild/src/features/apps/ai-photo-puter-generator.js
          grep -Fq "openWorkspace('ai-image')" "$V"
          grep -Fq "openWorkspace('templates')" "$V"
          grep -Fq "openWorkspace('projects')" "$V"
          grep -Fq 'stopImmediatePropagation' "$V"
          grep -Fq 'prompt.value.length' "$V"
          grep -Fq 'queueMicrotask' "$V"
          grep -Fq 'puter.ai.txt2img' "$P"
          grep -Fq 'Use in Design' "$G"
          grep -Fq 'Edit Photo' "$G"
          grep -Fq 'Download' fresh-rebuild/src/features/apps/ai-photo-phone-feedback-v1.js
          CSS=fresh-rebuild/assets/styles/ai-photo-route-fullscreen.css
          grep -Fq 'height: 100dvh' "$CSS"
          grep -Fq 'body.nx-ai-photo-route-active .nx-dock' "$CSS"
          grep -Fq '#nxEmergencySosButton' "$CSS"
          echo 'QA5 PASS — Home/back routing, Puter generation and fullscreen foundations preserved.'

      - name: 'QA 3–4 rendered — five Android viewport inspections'
        id: rendered
        shell: bash
        run: |
          set -euo pipefail
          bash tools/ai-photo-locked-visual/render-qa.sh

      - name: Upload rendered visual QA evidence
        if: always() && steps.rendered.outcome != 'skipped'
        uses: actions/upload-artifact@v4
        with:
          name: ai-photo-locked-render
          path: qa-artifacts/ai-photo-locked-render
          if-no-files-found: error
          retention-days: 7

      - name: Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: 'QA 6/6 — Android debug build + exact asset sync + no release'
        shell: bash
        run: |
          set -euo pipefail
          cd NexusNovaAndroid
          gradle --no-daemon clean assembleDebug
          test -s app/build/outputs/apk/debug/app-debug.apk
          cd ..
          for rel in \
            src/features/apps/ai-photo-locked-visual-v1.js \
            src/features/apps/ai-photo-locked-reference-assets-v1.js \
            src/features/apps/ai-photo-studio-review.js \
            src/features/apps/ai-photo-phone-feedback-v1.js \
            src/features/apps/ai-photo-puter-generator.js \
            src/features/apps/ai-photo-canva-workspace-v3.js \
            assets/visuals/ai-photo-locked-styles.webp \
            assets/visuals/ai-photo-locked-featured.webp \
            assets/visuals/ai-photo-locked-recent.webp \
            assets/styles/ai-photo-route-fullscreen.css; do
            cmp "fresh-rebuild/$rel" "NexusNovaAndroid/app/src/main/assets/www/$rel"
          done
          echo 'QA6 PASS — Android debug build + exact JS/reference-image bytes in Android assets. No OTA/APK publication.'
