name: NexusNova App Lock Security Hardening

on:
  push:
    branches:
      - main
    paths:
      - '.github/scripts/harden_app_lock.py'
      - '.github/scripts/harden_app_lock_runtime_test.py'
      - '.github/scripts/app-lock-security-readiness.py'
      - '.github/workflows/app-lock-security-hardening.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  harden-app-lock:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply App Lock hardening
        run: |
          python3 .github/scripts/harden_app_lock.py
          python3 .github/scripts/harden_app_lock_runtime_test.py

      - name: Validate App Lock security contract
        run: python3 .github/scripts/app-lock-security-readiness.py

      - name: Check JavaScript syntax
        run: |
          node --check js/nexusnova-security-lock-v1.js
          node --check .github/scripts/nexusnova-documents-vault-security-runtime.mjs

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Start local NexusNova origin
        shell: bash
        run: |
          printf '<!doctype html><meta charset="utf-8"><title>runtime</title>' > .runtime-origin.html
          python3 -m http.server 4173 > /tmp/nexusnova-app-lock-http.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/.runtime-origin.html >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/nexusnova-app-lock-http.log || true
          exit 1

      - name: Run App Lock browser regression
        run: node .github/scripts/nexusnova-documents-vault-security-runtime.mjs

      - name: Verify clean patch
        run: git diff --check

      - name: Commit App Lock hardening
        run: |
          if git diff --quiet -- js/nexusnova-security-lock-v1.js .github/scripts/nexusnova-documents-vault-security-runtime.mjs; then
            echo 'App Lock hardening already applied.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add js/nexusnova-security-lock-v1.js .github/scripts/nexusnova-documents-vault-security-runtime.mjs
          git commit -m 'Harden browser App Lock security'
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
