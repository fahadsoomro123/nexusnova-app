name: NexusNova Qibla Purple Gold V4 Review OTA

on:
  push:
    branches: [nova-sol57-pro-v128-qibla-purple-gold-v4]
    paths:
      - 'fresh-rebuild/index.html'
      - 'fresh-rebuild/assets/styles/batch3-tools-13-16-fullscreen.css'
      - 'fresh-rebuild/src/features/apps/batch3-tools-13-16-fullscreen.js'
      - 'fresh-rebuild/src/features/apps/premium-qibla-safe-v2.js'
      - 'fresh-rebuild/assets/qibla/**'
      - '.github/workflows/batch3-tools-13-16-review-ota.yml'
  workflow_dispatch:

permissions:
  contents: write

env:
  BATCH_PARENT: 0027773123694faedc9a129ef25ddaaf443ff1f8
  OTA_BUNDLED_BASE: 8d1aaaee6f28b974a8c8b3ff213beb1318ec7405
  PUBLIC_OTA_REPO: fahadsoomro123/nexusnova-website
  PUBLIC_OTA_BRANCH: nexusnova-ota-public

jobs:
  batch_review_ota:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout batch review branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify purple gold Qibla and protected logic
        shell: bash
        run: |
          set -euo pipefail
          JS=fresh-rebuild/src/features/apps/batch3-tools-13-16-fullscreen.js
          QIBLA=fresh-rebuild/src/features/apps/premium-qibla-safe-v2.js
          CSS=fresh-rebuild/assets/styles/batch3-tools-13-16-fullscreen.css
          INDEX=fresh-rebuild/index.html

          node --check "$JS"
          node --check "$QIBLA"
          test -s "$CSS"
          test -s "$INDEX"
          grep -Fq 'batch3-tools-13-16-fullscreen.css?ota=batch3-13-16-v1' "$INDEX"
          grep -Fq 'batch3-tools-13-16-fullscreen.js?ota=batch3-13-16-v1' "$INDEX"
          grep -Fq "id:'qibla'" "$JS"
          grep -Fq "id:'prayer-times'" "$JS"
          grep -Fq "id:'pakistan'" "$JS"
          grep -Fq "id:'news'" "$JS"
          grep -Fq "selectors:['[data-qb-heading]'" "$JS"
          grep -Fq "selectors:['[data-prayer-list]'" "$JS"
          grep -Fq "selectors:['[data-pk-list]'" "$JS"
          grep -Fq "selectors:['[data-news-list]'" "$JS"
          grep -Fq 'window.NexusNovaFresh?.openHub?.()' "$JS"
          grep -Fq "FACE_ASSET = './assets/qibla/qibla-purple-gold-face.webp'" "$QIBLA"
          grep -Fq "POINTER_ASSET = './assets/qibla/qibla-purple-gold-pointer.png'" "$QIBLA"
          grep -Fq 'startOrientationAutomatically' "$QIBLA"
          grep -Fq 'loadLocationAutomatically' "$QIBLA"
          grep -Fq 'maximumDelta = Math.max(6, 540' "$QIBLA"
          grep -Fq 'Math.abs(delta) <= .18' "$QIBLA"
          ! grep -Fq 'requestAnimationFrame' "$QIBLA"
          grep -Fq 'deviceorientationabsolute' "$QIBLA"
          grep -Fq '.nxq4-pointer' "$QIBLA"
          grep -Fq 'data-qb-bearing' "$QIBLA"
          grep -Fq 'data-qb-distance' "$QIBLA"
          ! grep -Fq 'data-qb-enable' "$QIBLA"
          ! grep -Fq 'data-qb-menu' "$QIBLA"
          ! grep -Fq 'data-qb-location' "$QIBLA"
          ! grep -Fq 'data-qb-coords' "$QIBLA"
          ! grep -Fq '>CALIBRATION<' "$QIBLA"
          ! grep -Fq '>HEADING<' "$QIBLA"
          ! grep -Fq '>MAGNETOMETER<' "$QIBLA"
          ! grep -Fq 'Field strength' "$QIBLA"
          ! grep -Fq 'Compass is ready' "$QIBLA"
          test -s fresh-rebuild/assets/qibla/qibla-purple-gold-face.webp
          test -s fresh-rebuild/assets/qibla/qibla-purple-gold-pointer.png
          test -s fresh-rebuild/assets/qibla/qibla-purple-gold-original.jpg
          test "$(sha256sum fresh-rebuild/assets/qibla/qibla-purple-gold-original.jpg | cut -d' ' -f1)" = d8efe7071ff34da620465ecbbb67430949fed7f15464c1122d460aed3a953c86
          python3 - <<'PY'
          import struct
          from pathlib import Path
          pointer=Path('fresh-rebuild/assets/qibla/qibla-purple-gold-pointer.png').read_bytes()
          assert pointer[:8] == b'\x89PNG\r\n\x1a\n'
          width,height,depth,color = struct.unpack('>IIBB',pointer[16:26])
          assert (width,height,depth,color) == (1024,1024,8,6)
          face=Path('fresh-rebuild/assets/qibla/qibla-purple-gold-face.webp').read_bytes()
          assert face[:4] == b'RIFF' and face[8:12] == b'WEBP'
          PY
          grep -Fq '.nx-b3-qibla-root' "$CSS"
          qibla_css="$(sed -n '/\/\* QIBLA/,/\/\* PRAYER TIMES/p' "$CSS")"
          test -n "$qibla_css"
          ! grep -Eq 'overflow[[:space:]]*:[[:space:]]*(auto|scroll)' <<<"$qibla_css"
          grep -Fq '.nx-b3-prayer-root' "$CSS"
          grep -Fq '.nx-b3-pakistan-root' "$CSS"
          grep -Fq '.nx-b3-news-root' "$CSS"
          grep -Fq 'height:100dvh!important' "$CSS"
          grep -Fq 'overflow:auto!important' "$CSS"

          # Batch #4 presentation/sensor work must not alter canonical app data logic.
          for file in \
            fresh-rebuild/src/features/apps/premium-qibla.js \
            fresh-rebuild/src/features/apps/premium-prayer-times.js \
            fresh-rebuild/src/features/apps/premium-prayer-safe.js \
            fresh-rebuild/src/features/apps/pakistan-suite.js \
            fresh-rebuild/src/features/apps/news-resilient.js \
            fresh-rebuild/src/features/apps/news-suite.js \
            fresh-rebuild/src/features/apps/app-screen.js \
            fresh-rebuild/src/main.js; do
            git diff --quiet "$BATCH_PARENT" "$GITHUB_SHA" -- "$file"
          done

          # Previously approved tools plus protected Mine/Drive remain byte-identical.
          for file in \
            fresh-rebuild/src/features/apps/profile-fullscreen-v1.js \
            fresh-rebuild/assets/styles/profile-fullscreen-v1.css \
            fresh-rebuild/src/features/apps/notes-fullscreen-v1.js \
            fresh-rebuild/assets/styles/notes-fullscreen-v1.css \
            fresh-rebuild/src/features/apps/todo-fullscreen-v1.js \
            fresh-rebuild/assets/styles/todo-fullscreen-v1.css \
            fresh-rebuild/assets/styles/todo-edit-v1.css \
            fresh-rebuild/src/features/apps/calculator-fullscreen-v1.js \
            fresh-rebuild/assets/styles/calculator-fullscreen-v1.css \
            fresh-rebuild/src/features/apps/batch1-tools-5-8-fullscreen.js \
            fresh-rebuild/assets/styles/batch1-tools-5-8-fullscreen.css \
            fresh-rebuild/src/features/apps/batch2-tools-9-12-fullscreen.js \
            fresh-rebuild/assets/styles/batch2-tools-9-12-fullscreen.css \
            fresh-rebuild/src/features/mine/mine-screen.js \
            fresh-rebuild/src/features/apps/premium-drive-native-v2.js \
            fresh-rebuild/src/features/apps/ai-photo-studio.js \
            fresh-rebuild/src/features/apps/ai-photo-studio-flagship.js \
            fresh-rebuild/src/features/apps/ai-photo-studio-review.js \
            fresh-rebuild/src/features/apps/premium-studio-suite.js; do
            git diff --quiet "$BATCH_PARENT" "$GITHUB_SHA" -- "$file"
          done

          allowed='^(.github/workflows/batch3-tools-13-16-review-ota.yml|fresh-rebuild/index.html|fresh-rebuild/assets/styles/batch3-tools-13-16-fullscreen.css|fresh-rebuild/src/features/apps/batch3-tools-13-16-fullscreen.js|fresh-rebuild/src/features/apps/premium-qibla-safe-v2.js|fresh-rebuild/assets/qibla/qibla-purple-gold-(face\.webp|pointer\.png|original\.jpg))$'
          unexpected="$(git diff --name-only "$BATCH_PARENT" "$GITHUB_SHA" | grep -Ev "$allowed" || true)"
          test -z "$unexpected" || { echo 'Unexpected Batch 13-16 changes:' >&2; printf '%s\n' "$unexpected" >&2; exit 1; }
          echo 'Purple/gold Qibla fullscreen + auto sensors + protected logic: PASS'

      - name: Build differential OTA preserving approved work
        shell: bash
        run: |
          set -euo pipefail
          BASE="$OTA_BUNDLED_BASE"
          git cat-file -e "$BASE^{commit}"

          deleted="$(git diff --name-only --diff-filter=D "$BASE" "$GITHUB_SHA" -- fresh-rebuild/ || true)"
          unexpected_deleted="$(printf '%s\n' "$deleted" | sed '/^$/d' | grep -Ev '^fresh-rebuild/src/emergency-sos\.js$' || true)"
          test -z "$unexpected_deleted" || { echo 'Unexpected deleted runtime files:' >&2; printf '%s\n' "$unexpected_deleted" >&2; exit 1; }

          rm -rf /tmp/nx-batch3-ota
          mkdir -p /tmp/nx-batch3-ota/files
          while IFS= read -r full; do
            [ -n "$full" ] || continue
            rel="${full#fresh-rebuild/}"
            mkdir -p "/tmp/nx-batch3-ota/files/$(dirname "$rel")"
            cp "$full" "/tmp/nx-batch3-ota/files/$rel"
          done < <(git diff --name-only --diff-filter=ACMRT "$BASE" "$GITHUB_SHA" -- fresh-rebuild/)

          for required in \
            index.html \
            assets/styles/profile-fullscreen-v1.css \
            assets/styles/notes-fullscreen-v1.css \
            assets/styles/todo-fullscreen-v1.css \
            assets/styles/todo-edit-v1.css \
            assets/styles/calculator-fullscreen-v1.css \
            assets/styles/batch1-tools-5-8-fullscreen.css \
            assets/styles/batch2-tools-9-12-fullscreen.css \
            assets/styles/batch3-tools-13-16-fullscreen.css \
            src/features/apps/profile-fullscreen-v1.js \
            src/features/apps/notes-fullscreen-v1.js \
            src/features/apps/todo-fullscreen-v1.js \
            src/features/apps/calculator-fullscreen-v1.js \
            src/features/apps/batch1-tools-5-8-fullscreen.js \
            src/features/apps/batch2-tools-9-12-fullscreen.js \
            src/features/apps/batch3-tools-13-16-fullscreen.js \
            src/features/apps/premium-qibla-safe-v2.js \
            assets/qibla/qibla-purple-gold-face.webp \
            assets/qibla/qibla-purple-gold-pointer.png \
            assets/qibla/qibla-purple-gold-original.jpg \
            src/features/apps/ai-photo-studio.js \
            src/features/apps/ai-photo-studio-flagship.js \
            src/features/apps/ai-photo-studio-review.js \
            src/features/apps/premium-studio-suite.js; do
            test -s "/tmp/nx-batch3-ota/files/$required"
          done

          export OTA_DIFF_BASE="$BASE"
          python3 - <<'PY'
          import hashlib,json,os
          from pathlib import Path
          root=Path('/tmp/nx-batch3-ota/files')
          files=[]
          for p in sorted(x for x in root.rglob('*') if x.is_file()):
              data=p.read_bytes()
              files.append({'path':p.relative_to(root).as_posix(),'sha256':hashlib.sha256(data).hexdigest(),'size':len(data)})
          paths={x['path'] for x in files}
          required={
              'index.html','assets/styles/profile-fullscreen-v1.css','assets/styles/notes-fullscreen-v1.css',
              'assets/styles/todo-fullscreen-v1.css','assets/styles/todo-edit-v1.css','assets/styles/calculator-fullscreen-v1.css',
              'assets/styles/batch1-tools-5-8-fullscreen.css','assets/styles/batch2-tools-9-12-fullscreen.css',
              'assets/styles/batch3-tools-13-16-fullscreen.css','src/features/apps/profile-fullscreen-v1.js',
              'src/features/apps/notes-fullscreen-v1.js','src/features/apps/todo-fullscreen-v1.js',
              'src/features/apps/calculator-fullscreen-v1.js','src/features/apps/batch1-tools-5-8-fullscreen.js',
              'src/features/apps/batch2-tools-9-12-fullscreen.js','src/features/apps/batch3-tools-13-16-fullscreen.js',
              'src/features/apps/premium-qibla-safe-v2.js','assets/qibla/qibla-purple-gold-face.webp',
              'assets/qibla/qibla-purple-gold-pointer.png','assets/qibla/qibla-purple-gold-original.jpg',
              'src/features/apps/ai-photo-studio.js',
              'src/features/apps/ai-photo-studio-flagship.js','src/features/apps/ai-photo-studio-review.js',
              'src/features/apps/premium-studio-suite.js'
          }
          assert required <= paths,(required-paths)
          assert 'src/emergency-sos.js' not in paths
          assert len(files) <= 2000
          assert sum(x['size'] for x in files) <= 60*1024*1024
          manifest={'schema':2,'base':os.environ['OTA_DIFF_BASE'],'version':os.environ['GITHUB_SHA'],'files':files}
          Path('/tmp/nx-batch3-ota/manifest.json').write_text(json.dumps(manifest,separators=(',',':'),sort_keys=True),encoding='utf-8')
          print('Purple/gold Qibla phone-review OTA files:',len(files))
          PY

      - name: Require public OTA credential
        shell: bash
        env:
          PUBLIC_OTA_TOKEN: ${{ secrets.GH_PUBLIC_OTA_TOKEN || secrets.OTA_PUBLIC_TOKEN }}
        run: test -n "$PUBLIC_OTA_TOKEN"

      - name: Publish public purple gold Qibla review OTA
        id: publish
        shell: bash
        env:
          PUBLIC_OTA_TOKEN: ${{ secrets.GH_PUBLIC_OTA_TOKEN || secrets.OTA_PUBLIC_TOKEN }}
        run: |
          set -euo pipefail
          rm -rf /tmp/nx-batch3-public
          git clone --depth 1 --branch "$PUBLIC_OTA_BRANCH" "https://github.com/$PUBLIC_OTA_REPO.git" /tmp/nx-batch3-public
          cd /tmp/nx-batch3-public
          git config user.name 'NexusNova OTA Bot'
          git config user.email 'actions@users.noreply.github.com'
          rm -rf ota && mkdir -p ota
          cp -a /tmp/nx-batch3-ota/. ota/
          git add -A ota
          git commit -m "Publish purple gold Qibla review OTA $GITHUB_SHA"
          PUBLIC_SHA="$(git rev-parse HEAD)"
          AUTH="$(printf 'x-access-token:%s' "$PUBLIC_OTA_TOKEN" | base64 -w0)"
          git -c http.extraheader="AUTHORIZATION: basic $AUTH" push origin HEAD:"$PUBLIC_OTA_BRANCH"
          echo "public_sha=$PUBLIC_SHA" >> "$GITHUB_OUTPUT"

      - name: Verify exact public purple gold Qibla OTA end-to-end
        shell: bash
        env:
          PUBLIC_SHA: ${{ steps.publish.outputs.public_sha }}
        run: |
          set -euo pipefail
          test -n "$PUBLIC_SHA"
          BASE_URL="https://raw.githubusercontent.com/$PUBLIC_OTA_REPO/$PUBLIC_SHA/ota"
          curl -fsSL "$BASE_URL/manifest.json" -o /tmp/public-batch3.json
          python3 - "$GITHUB_SHA" "$OTA_BUNDLED_BASE" <<'PY'
          import json,sys
          m=json.load(open('/tmp/public-batch3.json',encoding='utf-8'))
          expected,base=sys.argv[1],sys.argv[2]
          assert m.get('schema')==2 and m.get('base')==base and m.get('version')==expected
          paths={x.get('path') for x in m.get('files',[])}
          required={
              'index.html','assets/styles/batch1-tools-5-8-fullscreen.css','src/features/apps/batch1-tools-5-8-fullscreen.js',
              'assets/styles/batch2-tools-9-12-fullscreen.css','src/features/apps/batch2-tools-9-12-fullscreen.js',
              'assets/styles/batch3-tools-13-16-fullscreen.css','src/features/apps/batch3-tools-13-16-fullscreen.js',
              'src/features/apps/premium-qibla-safe-v2.js','assets/qibla/qibla-purple-gold-face.webp',
              'assets/qibla/qibla-purple-gold-pointer.png','assets/qibla/qibla-purple-gold-original.jpg',
              'src/features/apps/ai-photo-studio-flagship.js'
          }
          assert required <= paths
          assert 'src/emergency-sos.js' not in paths
          PY
          python3 - <<'PY'
          import hashlib,json,os,urllib.parse,urllib.request
          m=json.load(open('/tmp/public-batch3.json',encoding='utf-8'))
          public_sha=os.environ['PUBLIC_SHA']
          base=f'https://raw.githubusercontent.com/fahadsoomro123/nexusnova-website/{public_sha}/ota/files/'
          for item in m['files']:
              url=base+'/'.join(urllib.parse.quote(p,safe='') for p in item['path'].split('/'))
              data=urllib.request.urlopen(url,timeout=20).read()
              assert len(data)==item['size'],item['path']
              assert hashlib.sha256(data).hexdigest()==item['sha256'],item['path']
          print('Exact public purple gold Qibla review OTA verified:',len(m['files']),'files')
          PY
