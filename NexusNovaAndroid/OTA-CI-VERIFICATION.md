# OTA Engine Verification

This capability branch is verified by `NexusNova OTA Engine Capability CI` before the signed APK is delivered.

Required gates include native OTA unit tests, permanent release signing, certificate digest verification, package/version verification, Travel purge invariants, signed-APK publication metadata, and the downloadable signed artifact.
