# NexusNova Android

NexusNova Android is the native Android wrapper and deployment project for the NexusNova super app.

## Build

Use the GitHub Actions Android workflow or build locally from `NexusNovaAndroid/`.

## Web app

The Android app renders the production NexusNova web app and keeps a bundled offline shell for startup/recovery only.
