// NexusNova Travel edge backend URL.
// This remains empty until the Cloudflare Worker deploy returns its workers.dev URL.
// Keeping transport configuration in one tiny file lets us switch backends without
// touching the locked Travel V8 visual DOM/CSS.
export const TRAVEL_EDGE_URL = '';
