import fs from 'node:fs';
import {spawn} from 'node:child_process';

const [chromeBinary,driverBinary,serverPort,outDir]=process.argv.slice(2);
if(!chromeBinary||!driverBinary||!serverPort||!outDir)throw new Error('Usage: flagship-shell-v16-qa.mjs <chrome> <chromedriver> <server-port> <out-dir>');
fs.mkdirSync(outDir,{recursive:true});
const port=9533,log=fs.openSync(`${outDir}/chromedriver-shell-v16.log`,'a'),driver=spawn(driverBinary,[`--port=${port}`,'--allowed-ips=127.0.0.1'],{stdio:['ignore',log,log]});
const delay=ms=>new Promise(r=>setTimeout(r,ms));let sid='';const checks=[];
async function cmd(method,path,body){const r=await fetch(`http://127.0.0.1:${port}${path}`,{method,headers:body===undefined?undefined:{'content-type':'application/json'},body:body===undefined?undefined:JSON.stringify(body)}),raw=await r.text(),j=raw?JSON.parse(raw):{};if(!r.ok||j?.value?.error)throw new Error(`${method} ${path}: ${JSON.stringify(j.value||j)}`);return j.value}
async function ex(script,args=[]){return cmd('POST',`/session/${sid}/execute/sync`,{script,args})}
async function exa(script,args=[]){return cmd('POST',`/session/${sid}/execute/async`,{script,args})}
async function wait(script,label,timeout=16000){const st=Date.now();while(Date.now()-st<timeout){if(await ex(script))return;await delay(60)}throw new Error(`Timeout: ${label}`)}
async function click(selector){return ex('const n=document.querySelector(arguments[0]);if(!n)return false;n.click();return true;',[selector])}
async function shot(name){const png=await cmd('GET',`/session/${sid}/screenshot`);fs.writeFileSync(`${outDir}/${name}.png`,Buffer.from(png,'base64'))}
function rec(name,pass,evidence=''){checks.push({name,pass:Boolean(pass),evidence:String(evidence)});if(!pass)console.error(`::error::${name}: ${evidence}`)}
async function expect(name,script){try{const v=await ex(script);rec(name,!!v,JSON.stringify(v));return!!v}catch(e){rec(name,false,e.message);return false}}
async function start(){for(let i=0;i<60;i++){try{if((await cmd('GET','/status'))?.ready)break}catch{}await delay(100)}const made=await cmd('POST','/session',{capabilities:{alwaysMatch:{browserName:'chrome','goog:chromeOptions':{binary:chromeBinary,args:['--headless=new','--no-sandbox','--disable-dev-shm-usage','--force-color-profile=srgb'],mobileEmulation:{deviceMetrics:{width:393,height:852,pixelRatio:1,mobile:true,touch:true}}}}}});sid=made.sessionId;await cmd('POST',`/session/${sid}/timeouts`,{pageLoad:20000,script:20000,implicit:0});await cmd('POST',`/session/${sid}/url`,{url:`http://127.0.0.1:${serverPort}/tools/ai-photo-flagship-qa/harness.html`});await wait('return document.documentElement.dataset.qaReady||document.documentElement.dataset.qaError||"";','harness',20000);const err=await ex('return document.documentElement.dataset.qaError||"";');if(err)throw new Error(err)}
async function inject(){return exa(`const done=arguments[arguments.length-1],input=document.querySelector('[data-nxqt-file]');if(!input){done(false);return}const c=document.createElement('canvas');c.width=640;c.height=480;const x=c.getContext('2d');const g=x.createLinearGradient(0,0,640,480);g.addColorStop(0,'#20293c');g.addColorStop(1,'#e9b774');x.fillStyle=g;x.fillRect(0,0,640,480);x.fillStyle='#e7d6c8';x.beginPath();x.arc(320,210,112,0,Math.PI*2);x.fill();x.fillStyle='#2c1e22';x.fillRect(245,300,150,135);c.toBlob(b=>{const t=new DataTransfer();t.items.add(new File([b],'flagship-v16.png',{type:'image/png'}));input.files=t.files;input.dispatchEvent(new Event('change',{bubbles:true}));done(true)},'image/png');`)}

try{
  await start();
  await expect('V16 runtime, dark shell, touch smooth and Remove BG ML layers are installed','return window.__qaRoot?.dataset.aiPhotoFlagship==="flagship-repair-v16"&&window.__qaRoot?.classList.contains("nx-photo-v14")&&!!window.__qaRoot.__nxAiPhotoTouchSmoothV15&&!!window.__qaRoot.__nxAiPhotoRemoveBgMlV16&&!!document.getElementById("nx-ai-photo-flagship-shell-v14")&&!!document.getElementById("nx-ai-photo-touch-smooth-v15")&&!!document.getElementById("nx-ai-photo-remove-bg-ml-v16");');

  await click('[data-nxlock-quick="upscale"]');await wait('return window.__qaRoot.__nxQuickTools?.getState?.()?.screen==="picker";','upscale picker');await inject();await wait('return window.__qaRoot.__nxQuickTools?.getState?.()?.screen==="result";','upscale result');await click('[data-nxqt-edit]');await wait('const c=document.querySelector("[data-photo-canvas]");return c&&!c.hidden;','photo editor');
  await expect('Photo Editor chrome is visibly dark, not white-on-white','const f=document.querySelector(".nx-photo-frame"),t=document.querySelector(".nx-photo-top"),b=document.querySelector(".nx-photo-tools");if(!f||!t||!b)return false;return getComputedStyle(f).color.includes("247")&&getComputedStyle(t).backgroundImage.includes("linear-gradient")&&getComputedStyle(b).backgroundImage.includes("linear-gradient");');
  await expect('Bottom editor option labels are physically visible','const rgb=s=>(s.match(/\\d+(?:\\.\\d+)?/g)||[]).slice(0,3).map(Number),nodes=[...document.querySelectorAll(".nx-photo-tool span")];return nodes.length>=6&&nodes.every(n=>{const s=getComputedStyle(n),r=n.getBoundingClientRect(),c=rgb(s.color);return r.width>0&&r.height>0&&s.visibility!=="hidden"&&s.display!=="none"&&Number(s.opacity||1)>.6&&c.length===3&&c.reduce((a,v)=>a+v,0)>400});');
  await click('[data-photo-panel-open="adjust"]');await wait('return document.querySelector(".nx-photo-sheet")?.classList.contains("is-open")&&document.querySelector("[data-photo-sheet-panel=adjust]")?.classList.contains("is-active");','adjust sheet');
  await expect('Seven Light adjustment controls are physically displayed','const p=document.querySelector("[data-photo-sheet-panel=adjust].is-active"),fields=[...p.querySelectorAll("[data-photo-sub=light].is-active .nx-photo-field")];return fields.length===7&&fields.every(n=>{const r=n.getBoundingClientRect();return r.width>100&&r.height>20});');
  await expect('Seven active Light labels have readable dark-surface contrast','const p=document.querySelector("[data-photo-sheet-panel=adjust].is-active"),labels=[...p.querySelectorAll("[data-photo-sub=light].is-active .nx-photo-field span")],rgb=s=>(s.match(/\\d+(?:\\.\\d+)?/g)||[]).slice(0,3).map(Number);return labels.length===7&&labels.every(n=>{const c=rgb(getComputedStyle(n).color),r=n.getBoundingClientRect();return c.length===3&&c.reduce((a,v)=>a+v,0)>400&&r.width>20&&r.height>8});');
  await shot('phone-photo-editor-v16-adjust-393x852');

  const touch=await ex(`const i=document.querySelector('[data-photo-range="exposure"]'),top=document.querySelector('.nx-photo-top'),tools=[...document.querySelectorAll('.nx-photo-tool')],sheet=document.querySelector('.nx-photo-sheet');if(!i)return null;i.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,pointerId:71,pointerType:'touch',button:0,buttons:1}));i.focus();const s=n=>getComputedStyle(n);return{rootLive:window.__qaRoot.classList.contains('nx-photo-focus-editing'),topOpacity:Number(s(top).opacity),toolOpacity:Math.min(...tools.map(n=>Number(s(n).opacity))),sheetOpacity:Number(s(sheet).opacity)};`);rec('Slider touch keeps surrounding controls fully visible',touch&&touch.rootLive&&touch.topOpacity>.95&&touch.toolOpacity>.95&&touch.sheetOpacity>.95,JSON.stringify(touch));await ex(`const i=document.querySelector('[data-photo-range="exposure"]');i.dispatchEvent(new PointerEvent('pointerup',{bubbles:true,pointerId:71,pointerType:'touch',button:0,buttons:0}));i.blur();return true;`);

  const perf=await ex(`const i=document.querySelector('[data-photo-range="exposure"]');if(!i)return null;const start=performance.now();for(let n=0;n<60;n++){i.value=String(-1.5+(n%60)*.05);i.dispatchEvent(new Event('input',{bubbles:true}))}return{ms:performance.now()-start,value:i.value};`);rec('Rapid slider input returns without main-thread lock',perf&&perf.ms<120,JSON.stringify(perf));await delay(650);await expect('Editor remains responsive after slider burst','const s=document.querySelector(".nx-photo-sheet"),i=document.querySelector("[data-photo-range=exposure]");return s?.classList.contains("is-open")&&!!i&&!document.querySelector(".nx-photo-toast.is-on")?.textContent?.includes("error");');
  await expect('Photo Editor has no horizontal document overflow','return document.documentElement.scrollWidth<=innerWidth+1;');

  await ex('return window.__qaRoot.__nxStudioNavigation.showHome();');await wait('return !document.querySelector(".nxlock-home")?.hidden;','home');await click('[data-nxlock-quick="enhance"]');await wait('return window.__qaRoot.__nxQuickTools?.getState?.()?.screen==="picker";','enhance picker');await inject();await wait('return window.__qaRoot.__nxQuickTools?.getState?.()?.screen==="result";','enhance result');await expect('Enhance Pro controls are visible and compact','const p=document.querySelector(".nxfs-enhance-v12"),c=document.querySelector(".nxqt-canvas-wrap");if(!p||!c)return false;const pr=p.getBoundingClientRect(),cr=c.getBoundingClientRect();return pr.width>300&&pr.height>120&&cr.height<=innerHeight*.52;');await shot('phone-enhance-v16-393x852');
}catch(e){rec('Uncaught flagship shell v16 QA',false,e?.stack||e)}
finally{if(sid)try{await cmd('DELETE',`/session/${sid}`)}catch{}driver.kill('SIGTERM');fs.closeSync(log)}
const failed=checks.filter(x=>!x.pass);fs.writeFileSync(`${outDir}/flagship-shell-v16-report.json`,JSON.stringify({generatedAt:new Date().toISOString(),summary:{total:checks.length,passed:checks.length-failed.length,failed:failed.length},checks},null,2));if(failed.length){console.error(`Flagship shell v16 QA FAIL — ${failed.length}/${checks.length}`);process.exit(1)}console.log(`Flagship shell v16 QA PASS — ${checks.length} checks passed.`);
